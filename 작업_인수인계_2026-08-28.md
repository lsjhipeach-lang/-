# 삿포로 여행 앱 작업 인수인계

작성일: 2026-08-28  
기준 브랜치: `main`  
작성 시점 최신 기능 커밋: `de87013` (`Add bulk map place organizer`)

## 바로 접속할 주소

- 운영 앱: https://lsjhipeach-lang.github.io/-/
- GitHub 저장소: https://github.com/lsjhipeach-lang/-.git
- Supabase 프로젝트 ref: `rwmkfgnjsjfbipeybqqk`
- 여행 데이터 ID: `sapporo-2026`

## 새 컴퓨터에서 시작하기

Visual Studio Code, Git for Windows, Python 3, VS Code용 Codex 확장을 설치한 뒤 PowerShell에서 실행한다.

```powershell
cd "$HOME\Desktop"
git clone "https://github.com/lsjhipeach-lang/-.git" "삿포로 여행"
cd ".\삿포로 여행"
powershell -NoProfile -ExecutionPolicy Bypass -File ".\setup-new-pc.ps1"
code .
```

GitHub에 처음 push할 때는 Git Credential Manager 또는 VS Code에서 GitHub 로그인이 필요하다. 앱 데이터 확인은 배포 앱에서 본인 이메일로 Supabase 로그인을 완료한다. Supabase 함수나 DB 마이그레이션을 다시 배포할 때만 별도로 Supabase CLI 로그인이 필요하다.

```powershell
npx.cmd --yes supabase@2.72.7 login
npx.cmd --yes supabase@2.72.7 link --project-ref rwmkfgnjsjfbipeybqqk
```

인증 토큰, OTP, 비밀번호는 파일이나 GitHub에 저장하지 않는다.

## GitHub와 서버에 보존된 범위

- 앱 소스, 아이콘, PWA 파일, Supabase 함수·마이그레이션, 새 PC 설정 스크립트와 안내 문서는 GitHub에 있다.
- 실제 일정·경비·예약·체크리스트·가져온 장소는 소스 코드가 아니라 Supabase의 `trip_states`에 저장된다.
- Google Takeout 원본은 개인정보가 포함될 수 있어 GitHub에 올리지 않았다. 현재 앱에는 Google Maps 공유 목록을 통해 가져온 결과가 Supabase 데이터로 저장돼 있다.
- `supabase/.temp/`는 CLI 임시 캐시이므로 다른 PC에 가져갈 필요가 없고 GitHub에도 올리지 않는다.
- 이 PC에서 `sapporo-trip-user-manual.pdf`가 삭제 상태로 보이지만 원격 GitHub에는 보존돼 있다. 이 삭제는 의도 확인 전 커밋하지 않는다.

## 지금까지 완료한 핵심 작업

### 기본 앱과 모바일

- 일정, 예약, 단풍, 맛집, 술, 통합 지도, 경비, 체크리스트 화면 구성
- 모바일·PWA 설치 대응 및 안전한 모바일 미리보기 제공
- 현재 메뉴에 따라 상단 `+` 버튼의 추가 기능 변경
- 날짜·시간 키보드 입력 보정과 일정 작성 시 대시보드 장소 검색·불러오기
- 기능 연결 구조를 설명하는 온보딩 팝업과 `오늘 하루 보지 않기`

### 일정·예약·경비

- 예약 추가 창을 입력 없이 닫을 수 있도록 수정
- 예약 검색 연결과 예약 관리 화면 삭제 기능
- 일정·예약 상태 양방향 반영
- 경비 카테고리별 원형 비중 그래프와 필터
- 공동 지출, 개인 지출, `같이 사용 · 각자 결제` 입력 지원
- 기기 저장본·최근 로컬 백업·Supabase revision 병합으로 데이터 유실 방어

### 로그인과 협업

- 이메일 OTP 방식 Supabase 로그인
- 이승재·윤지원 두 사용자만 여행 데이터 접근 가능하도록 RLS 적용
- 두 기기 간 Supabase 저장 및 Realtime 동기화
- 동시에 수정했을 때 컬렉션 단위 병합과 충돌 재시도

### Google Maps 연동

- Google Maps 계정과 앱 로그인 계정이 달라도 사용 가능
- 공개 공유 목록 링크 또는 Takeout CSV·JSON 가져오기
- Google Places API, API 키, Cloud Billing을 사용하지 않는 요금 0원 구조
- 홋카이도 장소만 선택하며 불확실한 위치는 임의 좌표로 표시하지 않음
- 같은 공유 목록을 다시 가져올 때 기존 사용자 데이터 보존 및 중복 방지
- 임의 맛집·술 후보 12개 삭제, 단풍 스팟 유지
- 가져온 장소를 지도 상세에서 수정·삭제하고 맛집·술 데이터로 이동 가능
- 통합 지도에서 검색, 카테고리 필터, 전체 선택, 일괄 분류 가능

최근 관련 커밋:

- `de87013` 장소 목록 검색·선택·일괄 분류
- `ce334d9` 지도 장소 개별 관리·삭제·대시보드 이동
- `16e716a` Google Maps 전체 가져오기 및 초기 후보 정리
- `8ef3fb8` 공유 목록 링크 가져오기
- `099dc5a` 요금 0원 연동 구조

## 앞으로 진행할 작업

권장 순서: `3단계 → 4단계 → 7단계 → 5단계 → 6단계 → 8단계`

### 3단계 — 정보 보완함

- 좌표, 주소, 분류, 설명이 불완전한 장소만 필터링
- Google Maps 확인 후 저장하고 다음 미완성 장소로 바로 이동
- `위치 확인 필요`와 `정보 확인 필요`를 구분

### 4단계 — 중복·재가져오기 관리

- 재가져오기 결과에서 신규·업데이트·중복을 명확히 구분
- 중복 후보 비교 및 병합
- 기존 메모, 투표, 우선순위, 사용자가 수정한 분류를 항상 우선 보존

### 7단계 — 사용자용 백업·복구

- 앱 안에서 최근 서버·기기 백업 시점 확인
- JSON 내려받기와 복구 전 미리보기
- 복구 시 현재 상태를 먼저 한 번 더 백업

### 5단계 — 일정 연결 강화

- 날짜별 지도 핀과 일정 순서 표시
- 일정 작성 시 지역·카테고리·거리 기준 후보 찾기
- 유료 Google API 없이 저장된 좌표를 사용한 직선거리 계산

### 6단계 — 두 사람 후보 결정

- 각 사용자의 `무조건 가기 / 시간 되면 / 후보` 선택
- 의견 일치 장소 우선 표시
- 별도 채팅 같은 복잡한 기능은 추가하지 않음

### 8단계 — 최종 모바일 QA

- 두 계정으로 가져오기 → 분류 → 일정 → 예약·경비 → 동기화 전체 흐름 확인
- 320px, 360px, 390px, 430px, 태블릿, 데스크톱 점검
- 기존 기능을 유지하며 실제 사용을 막는 문제만 수정

## 다음 Codex 작업 요청문

```text
삿포로 여행 프로젝트 인수인계 문서를 읽고 3단계 정보 보완함부터 진행해줘.
기존 실제 데이터는 삭제하거나 초기화하지 말고, 수정 후 테스트·커밋·GitHub push·Pages 배포 확인까지 해줘.
사용자가 삭제 상태로 둔 PDF는 커밋하지 마.
```

## 작업·배포 안전수칙

1. 작업 시작 전에 `git pull --ff-only`와 `git status --short`로 상태를 확인한다.
2. 실제 사용자 데이터는 `initialData`로 덮어쓰거나 초기화하지 않는다.
3. Supabase 상태를 변경할 때 기존 데이터를 먼저 읽고 병합·백업한다.
4. `.env`, Supabase access token, GitHub token, OTP, Google 계정 자료를 커밋하지 않는다.
5. Google Places API와 Cloud Billing은 활성화하지 않는다.
6. 테스트 후 관련 파일만 명시적으로 `git add`하며 사용자 소유 변경은 제외한다.
7. 재배포 요청에는 항상 GitHub push까지 포함하고 Pages의 새 버전 반영도 확인한다.

