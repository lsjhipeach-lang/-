# 휴대폰에서 설치하는 가장 쉬운 방법

## 먼저 알아둘 것

- 노트북의 `localhost:8080` 주소는 노트북에서만 사용할 수 있습니다.
- 휴대폰 설치에는 GitHub Pages가 만들어 주는 `https://...` 주소가 필요합니다.
- 이 앱은 기존 Supabase의 `sapporo-2026` 여행 데이터를 그대로 사용합니다.

## 1. GitHub에 올리기

기존 GitHub 저장소의 웹 파일을 이 `삿포로 여행 앱 구현` 폴더의 내용으로 교체합니다. 폴더 자체를 한 단계 더 넣지 말고, GitHub 저장소 첫 화면에 `index.html`이 바로 보여야 합니다.

올려야 하는 항목:

- `.github`
- `icons`
- `.nojekyll`
- `index.html`
- `style.css`
- `pwa.css`
- `script.js`
- `manifest.webmanifest`
- `service-worker.js`
- `offline.html`

## 2. GitHub Pages 켜기

1. GitHub 저장소에서 `Settings`를 누릅니다.
2. 왼쪽에서 `Pages`를 누릅니다.
3. `Build and deployment`의 `Source`를 `GitHub Actions`로 선택합니다.
4. 저장소의 `Actions` 화면에서 `GitHub Pages 배포` 작업이 초록색 체크가 될 때까지 기다립니다.
5. `Settings → Pages`에 표시되는 `https://...` 주소를 복사합니다.

## 3. Supabase 로그인 주소 등록

로그인 이메일이 앱으로 돌아오게 하려면 Supabase 대시보드에서 다음을 설정합니다.

1. 기존 Supabase 프로젝트를 엽니다.
2. `Authentication → URL Configuration`으로 이동합니다.
3. `Site URL`에 GitHub Pages 주소를 넣습니다.
4. `Redirect URLs`에도 같은 주소와 주소 끝에 `/**`를 붙인 값을 추가합니다.

예시:

```text
https://사용자이름.github.io/저장소이름/
https://사용자이름.github.io/저장소이름/**
```

Supabase 프로젝트·테이블·키는 새로 만들지 않습니다.

## 4. 휴대폰에 설치하기

### Android

1. Chrome으로 GitHub Pages 주소를 엽니다.
2. 오른쪽 위 `⋮`를 누릅니다.
3. `앱 설치` 또는 `홈 화면에 추가`를 누릅니다.

### iPhone

1. Safari로 GitHub Pages 주소를 엽니다.
2. 아래의 공유 버튼을 누릅니다.
3. `홈 화면에 추가`를 누릅니다.

## 5. 두 기기 데이터 연결 확인

1. 노트북 앱에서 `서버 로그인`을 누르고 로그인합니다.
2. 휴대폰 앱에서도 `서버 로그인`을 누르고 로그인합니다.
3. 한 기기에서 체크리스트 하나를 변경합니다.
4. 다른 기기에 같은 변경이 나타나면 연결 완료입니다.

로그인하지 않은 상태의 변경은 그 기기에만 저장됩니다. 두 사람이 함께 사용하려면 두 기기 모두 서버 로그인을 완료해야 합니다.
