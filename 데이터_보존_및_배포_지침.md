# 데이터 보존 및 배포 지침

이 앱의 일정, 예약, 장소, 경비, 체크리스트는 코드가 아니라 사용자 데이터다. 기능 수정이나 재배포 과정에서 사용자 데이터를 초기 샘플 데이터로 교체하면 안 된다.

## 저장 위치

- 기본 저장소: 브라우저 `localStorage`의 `sapporo-trip-v3`
- 최근 정상 복구본: `sapporo-trip-v3-last-good`
- 순환 복구 이력: `sapporo-trip-v3-history`에 최근 12개
- 로그인 사용자 서버 저장소: Supabase `trip_states`, 여행 ID `sapporo-2026`
- `?preview=1`은 안전한 미리보기이며 입력 내용을 저장하지 않는다.

## 개발 시 필수 규칙

1. `sapporo-trip-v3` 저장 키를 바꾸거나 삭제하지 않는다.
2. 데이터 버전을 올릴 때 기존 객체를 버리고 `initialData`로 교체하지 않는다.
3. 마이그레이션은 최소한 `schedules`, `foliage`, `food`, `drinks`, `savedPlaces`, `reservations`, `expenses`, `checklist`, `activity`, `tripStart`, `tripEnd`, `exchangeRate`를 모두 보존한다.
4. 서버 데이터를 기기에 적용하기 전에 현재 기기 데이터를 로컬 복구 이력에 저장한다.
5. 기기와 서버 데이터는 `updatedAt` 또는 서버 `updated_at`을 비교하고 최신 데이터를 사용한다.
6. UI/CSS/서비스 워커 캐시 변경은 데이터 초기화를 필요로 하지 않는다. 캐시 삭제 코드와 `localStorage.clear()`를 함께 사용하지 않는다.
7. 사용자 요청 없이 Supabase의 `trip_states` 행을 삭제하거나 초기 샘플 데이터로 덮어쓰지 않는다.
8. 커밋에는 사용자 데이터 파일이나 비밀 키를 넣지 않는다.

## 배포 전 확인

1. 기존 데이터가 들어 있는 브라우저에서 현재 경비·일정·예약 건수를 기록한다.
2. 새 코드로 새로고침한 뒤 건수와 최근 입력 항목이 그대로인지 확인한다.
3. 로그인 상태에서 서버 동기화 후에도 같은 항목이 남아 있는지 확인한다.
4. 로그아웃 또는 오프라인 상태에서 입력한 뒤 새로고침해 데이터가 유지되는지 확인한다.
5. `?preview=1`에서 입력한 값은 저장되지 않는다는 안내가 보이는지 확인한다.
6. `node --check script.js`, `node --check service-worker.js`, `git diff --check`를 실행한다.
7. 앱 코드 파일만 명시적으로 커밋하고 사용자 소유의 다른 변경은 제외한다.
8. 푸시 후 공개 URL에서 새 스크립트 버전과 데이터 보존 여부를 다시 확인한다.

## 데이터가 사라져 보일 때 복구 순서

1. 주소에 `preview=1`이 있으면 실제 앱 주소로 다시 접속한다.
2. 설정에서 이전에 사용한 이메일 계정으로 서버 로그인한다.
3. 서버 상태 문구가 `서버 백업과 동기화됨`으로 바뀌는지 확인한다.
4. 앱은 기본 저장값이 손상되었을 때 최근 정상 로컬 복구본을 자동으로 사용한다.
5. 자동 복구가 안 되면 `sapporo-trip-v3-history`를 덮어쓰지 않도록 앱 사용을 중지하고 개발자가 복구 이력을 먼저 확인한다.
6. 서버 복구 시에는 `trip_states.updated_at`, 경비 건수, 최근 활동을 읽기 전용으로 확인한 뒤 사용자 확인 없이 데이터를 수정하지 않는다.
