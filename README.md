# SAPPORO / 4N5D — 여행 협업 대시보드

`index.html`을 열면 바로 실행되는 반응형 여행 컨트롤타워입니다. 별도 빌드 과정은 없습니다.

## 새 컴퓨터에서 바로 작업하기

새 컴퓨터에는 Visual Studio Code, Git for Windows, Python 3, VS Code용 Codex 확장을 설치합니다. 그다음 PowerShell에서 아래 명령을 실행합니다.

```powershell
cd "$HOME\Desktop"
git clone "https://github.com/lsjhipeach-lang/-.git" "삿포로 여행"
cd ".\삿포로 여행"
powershell -NoProfile -ExecutionPolicy Bypass -File ".\setup-new-pc.ps1"
code .
```

초기 설정 스크립트는 다음 항목을 자동으로 확인합니다.

- Git과 Python 설치 여부
- ZIP이 아닌 정상적인 Git 저장소인지
- `origin`이 이 프로젝트의 GitHub 저장소인지
- Git 커밋 작성자 이름과 이메일이 설정되어 있는지
- 로컬 변경이 없는 경우 최신 `main`을 안전하게 `pull`할 수 있는지

Git 사용자 정보가 없으면 스크립트가 이름과 이메일을 한 번 물어봅니다. GitHub 계정 인증은 첫 `push` 때 Git Credential Manager 또는 VS Code의 로그인 화면에서 진행합니다. 비밀번호나 토큰은 프로젝트 파일에 저장하지 않습니다.

VS Code에서 `Terminal → Run Task`를 열면 다음 작업도 사용할 수 있습니다.

- `Sapporo: 새 PC 초기 설정`
- `Sapporo: 로컬 서버 실행`

매 작업은 `git pull`로 시작하고, Codex에는 `수정 후 테스트하고 커밋·푸시까지 해줘`라고 요청하면 됩니다. Supabase URL·공개 키·테이블·여행 ID는 저장소 코드에 이미 연결되어 있으므로 새 PC에서 별도로 만들지 않습니다.

## PWA 실행 및 설치

PWA 기능은 보안 컨텍스트가 필요하므로 `file://`로 직접 열지 말고 이 폴더에서 로컬 웹 서버를 실행합니다.

```powershell
python -m http.server 8080
```

브라우저에서 `http://localhost:8080`을 연 뒤 Android Chrome의 설치 메뉴 또는 앱 안의 `앱 설치` 버튼을 사용합니다. iPhone Safari에서는 공유 버튼 → `홈 화면에 추가`를 선택합니다. 실제 다른 기기에서 접속·설치하려면 HTTPS로 배포해야 합니다.

서비스 워커는 앱의 HTML/CSS/JS/아이콘 등 정적 파일만 캐시합니다. Supabase 인증·데이터 API와 지도/CDN 요청은 캐시하지 않아 오래된 서버 데이터를 오프라인 화면에 고정하지 않습니다.

## 모바일 UI 미리보기와 QA

실제 서버나 DB에 연결하지 않고 모바일 앱 화면을 비교하려면 `mobile-preview.html`을 브라우저에서 직접 여세요.

```powershell
Start-Process ".\mobile-preview.html"
```

상단의 `320`, `360`, `390`, `430`, `768`, `1280` 버튼으로 기기 폭을 바꾸고, 화면 선택 메뉴로 실제 대시보드·일정·단풍·맛집·술·지도·경비·예약·체크리스트를 모두 열 수 있습니다. 앱 안의 메뉴·하단 내비게이션·카드·편집 모달도 실제 코드 그대로 동작합니다. `Debug 윤곽선`을 켜면 메뉴·하단 내비게이션·지도와 주요 요소 경계를 확인할 수 있고, 상단 상태가 현재 화면의 가로 넘침 여부를 표시합니다. 프리뷰의 추가·수정·삭제는 메모리에서만 반영되며 새로고침하면 초기화됩니다. Supabase 인증·API/DB 쓰기·서비스 워커·기존 localStorage는 실행하거나 변경하지 않습니다.

## 앱 구조

- 대시보드: D-Day, 확정/예약/비용/체크 현황, 5일 마스터 타임라인
- 일정: 날짜별 요약, 상세 편집, 드래그 순서 변경
- 장소: 단풍·맛집·술집 후보, 출처 링크, 투표 및 일정 추가
- 밤 동선: 스스키노 중심 Plan A/B, 도보권 미니 지도
- 통합 지도: 카테고리 필터와 장소 상세
- 운영: 경비, 예약 보드, 출발 전 체크, 최근 수정 내역

## 멤버와 권한

- 이승재: `MASTER` — 전체 편집 및 멤버 관리 권한
- 윤지원: 편집자 — 일정·장소·예약·경비·체크리스트 편집 권한

현재 멤버 전환 버튼으로 두 사용자 화면을 확인할 수 있습니다.

## 데이터 모델

상태는 `script.js`의 `initialData`를 기준으로 하며 다음 컬렉션으로 나뉩니다.

- `schedules`: 날짜, 시간, 장소, 카테고리, 이동·비용·예약·링크
- `foliage`: 평년 단풍 시기와 2026 현장 상태를 분리한 후보지
- `food`, `drinks`: 검증 정보, 우선순위, 투표, 지도·예약 링크
- `reservations`: 상태 보드와 일정 예약 상태 연동
- `expenses`: 공동/개인 구분, 결제자·개인 지출 대상, JPY/KRW 입력값, 환산 금액, 정산 상태
- `checklist`, `activity`: 공식 출처가 연결된 펼침형 준비 항목과 수정 이력

## 저장과 협업

- 로그인 전에는 `localStorage`에 저장되므로 현재 기기에서만 보입니다.
- 서버 로그인 후에는 기존 Supabase의 `trip_states` 테이블과 동기화됩니다.
- 노트북과 휴대폰 모두 서버 로그인하면 동일한 `sapporo-2026` 여행 데이터를 사용합니다.
- 한 기기에서 저장한 변경은 Supabase Realtime을 통해 다른 로그인 기기에 반영됩니다.
- 기존 Supabase URL, 공개 키, 인증 방식 및 DB 구조는 변경하지 않았습니다.

GitHub Pages 배포와 휴대폰 설치 과정은 [`모바일_배포_안내.md`](./모바일_배포_안내.md)를 따라 진행하세요.

## 화면 간 연동

- 공통 환율 → 메인 경비 카드, 경비 통계·내역·정산 원화 금액
- 일정 → 날짜별 보드, 마스터 타임라인, 예상 예산
- 예약 → 연결된 일정의 예약 상태·시간, 메인 예약 통계
- 경비 → 메인 경비, 공동·개인 합계, 1인당 평균, 개인별 실제 부담·정산
- 체크리스트 → 메인 할 일 수·미리보기, 체크 진행률
- 맛집·술집 → 후보 투표, 통합 지도와 스스키노 지도 팝업
- 멤버 전환 → 마스터/편집자 표시와 수정 이력 작성자

앱은 렌더링 후 위 관계의 데이터 무결성을 자동 진단하며 결과를 `body[data-sync-status]`와 `window.tripSyncReport`에 기록합니다.

장소의 2026 영업시간·가격·예약 여부가 검증되지 않은 경우 모두 `확인 필요`로 표시합니다. 미래 단풍과 날씨도 확정 정보로 취급하지 않습니다. 입국·의약품·의류 준비 항목은 일본 디지털청, 일본 세관·후생노동성, JNTO 공식 안내를 연결했습니다.
